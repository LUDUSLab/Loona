using UnityEngine;
using System.Collections;

public class PlayerInicialize : MonoBehaviour {

	// Use this for initialization
	void Start () {
       // Instantiate(CharacterSelection.personagens);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
